import { Box, styled } from '@mui/material';

export const Wrapper = styled(Box)({
  display: 'flex',
  alignItems: 'center',
  justifyContent: 'space-between',
  padding: '0rem 3rem',
  height: '8rem',
  width: '100%',
});
